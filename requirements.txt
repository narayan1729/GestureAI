# Edge-AI Accessibility Interface — Python dependencies
# Install with: pip install -r requirements.txt
#
# All processing is local / offline. No API keys required.

# Vision & face tracking
opencv-python>=4.8.0
mediapipe>=0.10.0

# UI framework
customtkinter>=5.2.0

# TTS (offline voice output)
pyttsx3>=2.90          # works out-of-the-box on Windows/macOS/Linux

# Optional: Piper TTS (better neural voice, see README for setup)
# pip install piper-tts  (requires additional ONNX model download)

# Standard library only — no extra installs needed for:
#   sqlite3, threading, queue, collections, math, time, subprocess, shutil
